# Pinned dependencies for building the MkDocs documentation site.
#
# Direct dependency is declared in docs/requirements.in (mkdocs-material).
# This file pins the full resolved tree so CI builds are reproducible and are
# not silently upgraded (see .github/workflows/deploy-docs.yml).
#
# Regenerate after changing requirements.in:
#   pip install pip-tools && pip-compile docs/requirements.in -o docs/requirements.txt
# or, without pip-tools:
#   python -m venv .venv && .venv/bin/pip install -r docs/requirements.in \
#     && .venv/bin/pip freeze --exclude pip --exclude setuptools --exclude wheel > docs/requirements.txt
#
# Dependabot keeps these up to date (pip ecosystem in .github/dependabot.yml).
babel==2.18.0
backrefs==6.2
certifi==2026.7.22
charset-normalizer==3.4.9
click==8.1.8
colorama==0.4.6
ghp-import==2.1.0
idna==3.18
jinja2==3.1.6
markdown==3.10.3
markupsafe==3.0.3
mergedeep==1.3.4
mkdocs==1.6.1
mkdocs-get-deps==0.2.2
mkdocs-material==9.7.7
mkdocs-material-extensions==1.3.1
packaging==26.2
paginate==0.5.7
pathspec==1.1.1
platformdirs==4.4.0
pygments==2.20.0
pymdown-extensions==10.21.3
python-dateutil==2.9.0.post0
pyyaml==6.0.3
pyyaml-env-tag==1.1
requests==2.32.5
six==1.17.0
urllib3==2.7.0
watchdog==6.0.0
